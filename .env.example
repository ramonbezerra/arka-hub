# Example env vars for docker-compose. Copy to .env and adjust for production.
POSTGRES_USER=arka
POSTGRES_PASSWORD=arkapass
POSTGRES_DB=arkahub
JWT_SECRET_KEY=change-me-in-production
REACT_APP_API_URL=http://localhost:5000
